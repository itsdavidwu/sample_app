<% provide(:title, 'All users') %>
<h1>All users </h1>

<%= will_paginate @users, renderer: BootstrapPagination::Rails %>

<ul class="users">
	<%= render user %>
</ul> 

<%= will_paginate @users, renderer: BootstrapPagination::Rails %>